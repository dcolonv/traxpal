export enum OrganizationTypeEnum {
  individual,
  basic,
  premium,
  enterprise,
}

export enum StatusEnum {
  active,
  removed,
}

export enum ClientProviderTypeEnum {
  client,
  provider,
}

export enum RuleTypeEnum {
  contains = 'contains',
  start_with = 'start_with',
}
